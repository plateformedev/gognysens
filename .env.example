# Database Configuration
DB_NAME=cognisens_db
DB_USER=cognisens
DB_PASSWORD=cognisens_password
DB_ROOT_PASSWORD=root_password

# WordPress Configuration
WP_DEBUG=true

# Development Mode (set to false for production)
# When true: adds noindex, disables robots
COGNISENS_DEV_MODE=true

# Site URL (for production)
SITE_URL=https://cognisens.fr

# Permalink Structure
WP_PERMALINK=/%postname%/
